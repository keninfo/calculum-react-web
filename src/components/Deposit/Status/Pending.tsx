import React from 'react'

import ActionAlert from '@/components/common/ActionAlert'

const Pending = () => {
  return <ActionAlert alert="You have a deposit pending, wait at least one epoch for it to be reflected." />
}

export default Pending
